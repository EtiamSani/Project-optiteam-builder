services:
    db:
      image: mysql
      environment:
        MYSQL_ROOT_PASSWORD: optiteam
    phpMyAdmin:
      image: phpmyadmin
      environment:
        PMA_ARBITRARY: 1
      ports:
        - "8080:80"